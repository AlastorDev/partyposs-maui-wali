{
  "name": "partyposs-maui-wali",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A complete Wood Badge critter adventure with catching, patrol battles, trials, and PartyPoss.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/core.test.mjs tests/world.test.mjs",
    "check": "node --check dist/game.js && node --check dist/core.js && node --check dist/world.js && node --check dist/adventure.js && node --check dist/art.js && node --check dist/sw.js"
  }
}
